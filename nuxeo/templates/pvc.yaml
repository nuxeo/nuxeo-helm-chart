{{- if .Values.nuxeo.logs.persistence.enabled }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ template "nuxeo.fullname" . }}-log
  labels:
    app: {{ template "nuxeo.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
{{- with .Values.nuxeo.logs.persistence.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
  annotations:
  {{- if .Values.nuxeo.logs.persistence.storageClass }}
    volume.beta.kubernetes.io/storage-class: {{ .Values.nuxeo.logs.persistence.storageClass | quote }}
  {{- else }}
    volume.alpha.kubernetes.io/storage-class: default
  {{- end }}
  {{- with .Values.nuxeo.logs.persistence.annotations }}
  {{ toYaml . | indent 4 }}
  {{- end }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ required "nuxeo.logs.persistence.size is required" .Values.nuxeo.logs.persistence.size | quote }}
{{- end }}
